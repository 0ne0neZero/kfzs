<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.finance.domains.configure.subject.repository.mapper.SubjectCashFlowMapper">

    <select id="selectByMapDetail" resultType="com.wishare.finance.domains.configure.subject.entity.SubjectCashFlow">
        select scf.*
        from subject_cash_flow scf
                 inner join subject_map_unit_detail smud on smud.subject_level_last_id = scf.subject_id
        where
          scf.subject_id = #{subjectId}
          and smud.sub_map_unit_id = #{mapUnitId}
          and smud.map_type = #{mapType}
    </select>
</mapper>
