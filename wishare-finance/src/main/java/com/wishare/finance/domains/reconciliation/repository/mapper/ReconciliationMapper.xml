<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.finance.domains.reconciliation.repository.mapper.ReconciliationMapper">

    <select id="getReconciliationClearStatistics" resultType="com.wishare.finance.apps.model.reconciliation.vo.ReconciliationClearStatisticsV">
        select
            IFNULL( sum(actual_total) , 0 ) as actualTotal,
            IFNULL( sum(flow_claim_total), 0) as flowClaimTotal,
            IFNULL( sum(commission), 0) as commission
            from reconciliation
            ${ew.customSqlSegment}
    </select>



    <select id="getReconciliationStatistics" resultType="com.wishare.finance.apps.model.reconciliation.vo.ReconciliationStatisticsV">
        select
            IFNULL( sum(actual_total) , 0 ) as actualTotal,
            IFNULL( sum(flow_claim_total), 0) as flowClaimTotal,
            IFNULL( sum(invoice_total), 0) as invoiceTotal
        from reconciliation
                 ${ew.customSqlSegment}
    </select>

</mapper>
