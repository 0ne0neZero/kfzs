<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.finance.domains.configure.chargeitem.repository.mapper.TaxItemGoodsMapper">

    <sql id="tableFields" >
         tig.id,
         tig.goods_name,
         tig.operator_name,
         tig.gmt_modify,
         ti.code,
         ti.name
    </sql>

    <select id="queryTaxItemGoodsByPage" resultType="com.wishare.finance.domains.configure.chargeitem.dto.taxitem.TaxItemGoodsD">
        select
        DISTINCT
        <include refid="tableFields" />
        FROM
        tax_item_goods tig
        INNER JOIN tax_charge_item_relation tcir ON tig.id = tcir.tax_item_goods_id
        INNER JOIN tax_item ti on ti.id=tcir.tax_item_id
        ${ew.customSqlSegment}
    </select>
</mapper>

