<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.finance.domains.configure.subject.repository.mapper.AssisteBizTypeMapper">

    <insert id="saveOrUpdateBatchCode">
        insert into assiste_biz_type (
        id,
        code,
        name,
        asc_code,
        asc_name,
        parent_code,
        disabled,
        org_id,
        org_code,
        org_name,
        sync_system,
        creator,
        creator_name,
        gmt_create,
        operator,
        operator_name,
        gmt_modify) values
        <foreach collection="abt" separator="," item="item" open="" close="">
            (
             #{item.id},
             #{item.code},
             #{item.name},
             #{item.ascCode},
             #{item.ascName},
             #{item.parentCode},
             #{item.disabled},
             #{item.orgId},
             #{item.orgCode},
             #{item.orgName},
             #{item.syncSystem},
            #{item.creator},
            #{item.creatorName},
            #{item.gmtCreate},
            #{item.operator},
            #{item.operatorName},
            #{item.gmtModify})
        </foreach>
        on duplicate key update
            name = values(name),
            asc_code = values(asc_code),
            asc_name = values(asc_name),
            parent_code = values(parent_code),
            disabled = values(disabled),
            org_id = values(org_id),
            org_code = values(org_code),
            org_name = values(org_name),
            sync_system = values(sync_system),
            operator = values(operator),
            operator_name = values(operator_name),
            gmt_modify = values(gmt_modify)
    </insert>

    <select id="selectAssisteItems" resultType="com.wishare.finance.domains.configure.subject.entity.AssisteItemOBV">
        select 4 as `type`,
               code,
               name,
               asc_code,
               asc_name
        from assiste_biz_type
        where code like CONCAT('%', #{code}, '%')
           or name like CONCAT('%', #{name}, '%') limit 500
    </select>
</mapper>
