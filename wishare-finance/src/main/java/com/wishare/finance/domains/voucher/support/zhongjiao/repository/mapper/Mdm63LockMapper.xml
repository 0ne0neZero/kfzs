<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.finance.domains.voucher.support.zhongjiao.repository.mapper.Mdm63LockMapper">

    <delete id="deleteByVoucherBillNo">
        delete from mdm63_lock
        where voucher_bill_no = #{voucherBillNo}
    </delete>

    <!--根据报账单ID删除锁定MDM63数据-->
    <update id="updateDeletedByVoucherBIllId">
        delete from mdm63_lock
        WHERE voucher_bill_id = #{voucherBillId}
    </update>

    <delete id="deleteMdm63Lock">
        delete from mdm63_lock
        where ft_id = #{ftId}
            AND voucher_bill_no = #{voucherBillNo}
    </delete>
</mapper>
