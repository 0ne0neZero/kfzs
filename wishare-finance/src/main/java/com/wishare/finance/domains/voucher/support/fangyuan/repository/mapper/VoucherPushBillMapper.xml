<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.finance.domains.voucher.support.fangyuan.repository.mapper.VoucherPushBillMapper">

    <delete id="delete">
        delete from voucher_bill
        where id = #{id}
    </delete>


    <select id="selectBySearch"
            resultType="com.wishare.finance.domains.voucher.support.fangyuan.entity.VoucherBill">
            select * from voucher_bill
             ${ew.customSqlSegment}
    </select>

    <select id="getMoney" resultType="com.wishare.finance.apps.pushbill.vo.VoucherBillMoneyV">
        select sum(total_amount)/100 as money
        from voucher_bill
        ${ew.customSqlSegment}
    </select>
</mapper>
