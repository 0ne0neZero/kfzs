<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wishare.contract.domains.mapper.contractset.ContractPaymentDetailMapper">
    <!-- 表字段展示 ,xml不会在表字段更新后更新，
    com.wishare.contract.domains.consts.contractset.ContractPaymentDetailFieldConst 会每次重新生成
    可以从javadoc中获取到最新的表字段列表-->
    <!-- id,contractId,collectionPlanId,paymentAmount,applyPaymentTime,actualPaymentTime,userId,userName,
    remark,paymentType,invoiceUrl,invoiceCode,auditStatus,auditCode,paymentStatus,
    confirmStatus,deleted,creator,creatorName,gmtCreate,operator,operatorName,
    gmtModify -->

    <delete id="deleteByCollectionPlanId">
        UPDATE contract_payment_detail
            SET deleted = 1
        WHERE
            collectionPlanId = #{collectionPlanId}
    </delete>

    <delete id="deleteByContractId">
        UPDATE contract_payment_detail
            SET deleted = 1
        WHERE
            contractId = #{contractId}
    </delete>

    <select id="selectCountCurrentDate" resultType="java.lang.Long">
        SELECT
            COUNT(1)
        FROM
            contract_payment_detail
        WHERE
            DATE_FORMAT(gmtCreate,"%Y-%m-%d") = DATE_FORMAT(NOW(),"%Y-%m-%d")
    </select>

    <select id="selectOneByBpmRecordId" resultType="java.lang.Long">
        select id
        from contract_payment_detail
        where deleted = 0
          and procId = #{recordId}
            limit 1
    </select>
</mapper>
